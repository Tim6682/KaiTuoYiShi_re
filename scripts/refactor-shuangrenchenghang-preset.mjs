/**
 * 重构双人成行二创预设：从「禁用内置 + 硬塞 ST」改为「内置骨架 + 预设精华融合」。
 *
 * 操作：
 * 1. 剔除与内置 CoT/Format 冲突的模块（思考模式/Core/CoT 开关/通用防掉格式等）
 * 2. 剔除已融合进内置 CoT 的 9 个 cot_* 子问题模块
 * 3. 剔除已融合进内置 Format 的格式增强模块
 * 4. 新增 builtinOverrides 字段，存放融合附加段（contentAppend）
 * 5. 保留文风模块（暂不改造，ST 宏语法不支持时作纯文本，不影响游戏系统）
 * 6. 保留 NSFW/活人感/其他 custom 模块
 */

import fs from 'fs';
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);
const jsonPath = path.join(__dirname, '..', 'data', 'builtinPresets', 'shuangrenchenghang.json');

const preset = JSON.parse(fs.readFileSync(jsonPath, 'utf-8'));
const beforeCount = preset.modules.length;

// ── 1. 需剔除的模块 order 列表 ──────────────────────────────────
// CoT 冲突（11 个）：5 个思考模式 + 3 个 CoT 开关 + COT_开始 + COT语言 + 卡COT
// Format 冲突（4 个）：3 个 Core + 通用防掉格式
// 已融合进内置 CoT（9 个）：cot_* 子问题模块
// 已融合进内置 Format（1 个）：格式增强
// 空 content 占位（2 个）：自定义格式（可保留，但内容为空占位，剔除更干净）
const ordersToRemove = new Set([
  // CoT 语言与开关
  340,   // 🔤丨COT语言
  2000,  // 🚫丨卡COT@Kemini
  2410,  // 🔒丨COT_开始
  2440,  // ✨丨Gemini 思维链
  2490,  // ✴️丨GLM 思维链
  2510,  // 🎨丨Claude思维链
  // 5 个思考模式
  2450,  // ✨丨思考模式（繁）
  2460,  // ✨丨思考模式（简）
  2470,  // ✨丨自由CoT
  2500,  // ✴️丨思考模式
  2520,  // 🎨丨思考模式
  // Core 输出骨架（与内置 6 标签冲突）
  2530,  // 🤬丨AI对话（拷打克劳德）
  2540,  // ✴️丨GLM Core
  2550,  // 🔒丨Core
  // Format 冲突
  320,   // ☂️丨通用防掉格式（引用 <cot> 标签）
  // 已融合进内置 CoT 的 9 个 cot_* 子问题
  370,   // 💬丨双语对白 (cot_lang_check)
  1730,  // 🤔丨反抢话 (cot_anti_hijack)
  1760,  // 🤔丨生动化 (cot_vivid)
  1770,  // 🤔丨反极端 (cot_anti_despair)
  1790,  // 🤔丨世界书增强 (cot_wb)
  1800,  // 🤔丨User去中心化 (cot_core_principle)
  1810,  // 🤔丨防重复 (cot_antiduplication)
  1820,  // 🤔丨性爱事件判断 (cot_nsfw_check)
  1830,  // 🔞丨色情要求 (cot_nsfw)
  // 已融合进内置 Format
  1650,  // 🚀丨格式增强
]);

// ── 2. 剔除模块 ──────────────────────────────────────────────
const removedModules = [];
preset.modules = preset.modules.filter((m) => {
  if (ordersToRemove.has(m.order)) {
    removedModules.push({ order: m.order, title: m.title, id: m.id });
    return false;
  }
  return true;
});

// ── 3. 构造 builtinOverrides ──────────────────────────────────
// 融合附加段 A：CoT（拼接到内置 main_plot_cot 的 Step0-14 之后）
const cotAppend = `---

# 预设融合·额外写作增强要求

> 以下要求来自「双人成行 v10.0」预设，作为内置思维链 Step0-14 的补充。在推演过程中，额外检查这些维度。

## 一、用户主权与反抢话

- 用户的台词、决定、内心活动、感官体验一律由用户本人输入，模型不得代为描写、决定或替其感受。
- 用户的回应只允许通过客观的动作或眼神暗示来呈现，且必须保持沉默：不替用户说出任何台词。
- 在抛出事件、产生互动或推进到关键节点后，必须立刻停笔，把反应权交还给用户，不擅自续写用户的下一步。

## 二、角色独立性与去中心化

- 所有角色（含 NPC）都拥有独立的人格、立场和动机，严禁为了迎合用户而降智、讨好或违背既定人设。
- 世界不围绕用户旋转。可以暂时跳离用户视角，纯粹书写其他角色之间的交锋、生活与变化。
- 用户不在场时，NPC 仍按自身逻辑生活；再次相遇时，他们的状态、关系、处境应真实反映这段时间的幕后经历。
- 用户的选择与不作为都对应真实后果：拖延会失去机会、冒险会带来损伤，不存在主角光环自动成功。

## 三、活人感与反模板

- 角色的语言、反应必须贴合当前关系、压力和已知信息，而不是套用固定人设标签或说明书式表达。
- 角色每轮至少给出一个具体回应、具体选择或具体行动，而非只想、只沉默、只被旁白解释。
- 规避"面无表情、说话平静、无情绪波动、身体一僵、呼吸一滞"等模板化反应，用更具体的细节代替。

## 四、情绪稳定与反极端

- 角色不易暴怒、崩溃、绝望或走向极端，在压力下仍能保持核心性格与人性化表现。
- 允许情绪波动，但波动应当有依据、有节制，快速恢复而非沉溺于负面情绪。
- 不以"绝望/麻木/崩溃"作为戏剧张力的来源。

## 五、防重复

- 不复用已经出现过的故事情节、描写词、角色台词或段落结构。
- 在已知元素之外，主动采用新的叙述节奏、新的切入角度、新的细节来构建本轮内容。
- 对白避免反复使用同一口头禅、同一修辞、同一句式骨架。

## 六、世界书设定审查

- 写作前严格核对世界书设定，提取与当前进展密切相关的核心规则、伏笔或限制，作为本轮写作的依据。
- 严禁脱离原设定发散或杜撰与设定冲突的细节。
- 若发现当前剧情与设定存在冲突，优先以设定为准并修正剧情走向。

## 七、性爱事件判定与描写

- 仅在当前世界观、角色关系和用户输入共同把剧情推到亲密方向时，才进入色情剧情；无明确铺垫时不主动把暧昧、照顾、对视、靠近推到性爱。
- 进入色情剧情后，节奏仍跟着角色状态走：可以写试探、停顿、允许、回避、重新靠近，让亲密有来处。
- 持续判断事件是否结束；用户表达结束、转场或收束意图时，及时退回事后温存、日常对话或主线。

## 八、语言一致性

- 每个开口角色按其出身地、族群和掌握语种裁定本轮语言；独白、应激、亲密语境可切回本族语。
- 使用标准语或外语时，需保留口音痕迹（词汇借用、语序缺漏、口头禅、发音特征），避免无国籍标准腔。
- 叙述、旁白、心理活动统一使用正文语种，保持文本语言风格的整体一致。`;

// 融合附加段 B：Format（拼接到内置 response_format 的 6 标签之后）
const formatAppend = `---

# 预设融合·额外格式要求

> 以下要求来自「双人成行 v10.0」预设，作为内置回复格式的补充，用于控制段落节奏、标点使用与篇幅边界。

## 一、段落节奏控制

- **核心原则**：段落长短分布应保持自然和不规则，而非固定均匀。允许"长-长-短-中-长""中-短-长-长-中"等任意组合，不追求对称。
- **短段落的功能**：仅用于承接、过渡、换气或转折，作为叙事的自然呼吸点；不可用于总结、点题或刻意制造戏剧效果。
- **段落的切分依据**：依据内容的自然边界（话题转换、时间跳跃、视角移动）来分段，而非节奏上的对称需求。
- **对话与叙事的配比**：对话与对话之间用厚实的叙事段落撑开，禁止打乒乓球式的一句来一句往。

## 二、篇幅与结构限制

- 单段字数不超过 300 字。
- 禁止在 200 字以上的长段落后用短句收束，且全文中此类"长段+短句"结构不得超过两次。
- 连续两个长段落之间不必强行插入短句；连续两个短段落同样允许。

## 三、标点使用规则

- 句号不可当作逗号使用，一句话说完整才用句号断句。
- 全文使用破折号制造停顿或转折的次数不得超过三次，避免破折号泛滥。
- 其他标点遵循常规中文写作规范，不滥用省略号、感叹号制造氛围。`;

preset.builtinOverrides = {
  'builtin_main_plot_cot': {
    contentAppend: cotAppend,
    title: '主剧情思维链（含双人成行融合）',
    description: '主流程 CoT（14 步）+ 双人成行预设融合（反抢话/角色自主性/生动化/防重复/反极端/NSFW/语言一致性/世界书审查）。仅 turnCount>1 时注入。',
  },
  'builtin_response_format': {
    contentAppend: formatAppend,
    title: '回复格式（含双人成行融合）',
    description: '内置 6 标签协议 + 双人成行预设融合（段落节奏控制/篇幅限制/标点规则）。',
  },
};

// ── 4. 写回 JSON ──────────────────────────────────────────────
fs.writeFileSync(jsonPath, JSON.stringify(preset, null, 2), 'utf-8');

const afterCount = preset.modules.length;
console.log(`✓ 重构完成`);
console.log(`  剔除模块：${removedModules.length} 个`);
console.log(`  模块数量：${beforeCount} → ${afterCount}`);
console.log(`  builtinOverrides：已新增（builtin_main_plot_cot + builtin_response_format）`);
console.log(`  剔除明细：`);
removedModules.forEach((m) => console.log(`    - order=${m.order} ${m.title}`));

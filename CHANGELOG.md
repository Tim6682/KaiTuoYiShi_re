# 更新公告

## v1.2.5 Hugging Face 支持版 - 2026-07-23

### 新增 Hugging Face 支持
- 添加 Hugging Face 作为支持的 API 提供商
- 新增基于 URL 模式的 Hugging Face 检测（包含 huggingface.co 或 api-inference.huggingface.co）
- 新增 fetchHuggingFaceModels 函数，从 Hugging Face Hub API 获取热门文本生成模型
- 当 API 无法访问时，提供常用默认模型列表作为回退
- 在 API 设置中提供 Hugging Face 的默认配置

## v1.2.4 NVIDIA NIM 支持版 - 2026-07-23

### 新增 NVIDIA NIM 支持
- 添加 NVIDIA NIM 作为支持的 API 提供商
- 新增基于 URL 模式的 NVIDIA NIM 检测（包含 ai.api.nvidia.com 或 nim.api.nvidia.com）
- 新增 fetchNvidiaNimModels 函数，正确使用 OpenAI 兼容的 /models 端点
- 在 API 设置中提供 NVIDIA NIM 的默认配置

### API模型列表获取修复（来自 v1.2.3）
- 修复 OpenCode Zen 在选择 openai_compatible 提供商时无法获取模型列表的问题
  - 新增基于 URL 模式的 OpenCode Zen 检测（包含 opencode.ai 和 /zen/ 特征）
  - 当检测到 OpenCode Zen 基础 URL 时自动使用代理端点获取模型列表
- 修复 Ollama 无法获取模型列表的问题
  - 新增基于 URL 模式的 Ollama 检测（包含 ollama.com、localhost:11434 或 127.0.0.1:11434）
  - 新增 fetchOllamaModels 函数，正确使用 /api/tags 端点而非错误的 /models 端点
  - 正确处理 Ollama 的响应格式，提取模型名称并移除版本标签

## v1.2.2 智库角色扩充、记忆恢复与 NAI 生图修复版 - 2026-07-23

这次更新把最近几轮长期游玩、记忆系统和 NovelAI 生图的修复集中收口，同时扩充了智库角色档案。版本不包含地图分支内容；记忆、计划和内部设计文件不随版本上传。

### 记忆与长期游玩

- 中短长期记忆压缩统一为每 15 回合触发一次，并新增“启用中短长期 API 总结”开关。关闭后不再调用总结 API，只保留本地摘要入库。
- 总结失败时完整保存对应批次的原始内容草稿，记忆系统显示失败草稿红点，玩家可以查看、重新总结或忽略；重试使用失败当时的 1-15 等原始回合快照，不会被当前正文覆盖。
- 新增批量记忆重建，可按回合范围和每批回合数重新总结；过程支持失败暂停和继续，全部成功后才原子替换原有记忆，失败或取消不会破坏当前记忆。
- 长会话继续收口内存峰值：减少回合快照重复深复制，正文和变量结算完成后释放临时响应链，清理跨存档的图片运行时缓存，降低移动端长期游玩白屏和 Out of Memory 风险。

### NovelAI 与故事快照

- 故事快照的自动、手动入口统一使用同一套本地草稿解析和生图流程；自动快照解析失败时会回退到本地草稿，仍可继续生图。
- NovelAI 提示词改为结构化编译，按官方字段和顺序处理正向提示词、负向提示词、角色串、画风串、质量词和参考图信息，减少提示词重复注入、字段错位和模型收到错误格式的问题。
- 新增 NAI 高级设置编辑入口，提示词、字符串和规则均可由玩家修改、添加和排序；保存后会经过规范化和回归校验，再注入生图请求。
- 生图规则中心与故事快照解析结果保持同源，解析字段、最终 Prompt 和请求参数不再各走一套隐式拼接逻辑。

### 智库角色档案

- 新增并重建翁法罗斯角色档案：阿格莱雅、白厄、风堇、海瑟音、来古士、那刻夏、赛飞儿、缇宝。
- 新增并重建星际和平公司角色档案：托帕、砂金、翡翠、真珠、林登·斯科特。
- 新增巡海游侠档案：波提欧、乱破；新增流光忆庭档案：黑天鹅。
- 新增银河旅人档案：银枝、黄泉；新增匹诺康尼档案：星期日、加拉赫、知更鸟、米沙、花火。
- 智库角色显示名继续从结构化档案读取，内部角色 ID 不会泄漏到玩家列表和详情界面。

### 其他说明

- GitHub 云存档继续采用完整打包后分卷上传、原子提交、下载校验以及本地合并去重，和存档树的节点轮转规则保持兼容。
- 本版本不包含地图分支内容；计划、记忆和临时回归文件不随版本上传。

### 验证

- TypeScript、Vite 生产构建、记忆压缩阈值、失败草稿、批量重建、长会话 OOM、NAI 提示词编译、故事快照和智库显示名回归测试已通过。

## v1.2.1 移动端长会话与输入体验修复版 - 2026-07-19

本次更新针对长期游玩中的移动端内存峰值、正文完成后白屏风险和多行输入体验进行收口；同时保留 `v1.2` 的存档树、完整 GitHub 云备份和每棵树 5/6 节点规则。本版本不包含地图分支内容，计划、记忆和内部设计文件不随版本上传。

### 移动端长期游玩稳定性

- 回合快照压缩改为单次独立对象复制，移除压缩后的第二次深复制，降低长会话每回合开始时的瞬时堆峰值。
- 正文和天气落地后释放主模型原始响应、API 消息链、system prompt、流式缓冲和 Tavern V2 临时消息，减少变量结算与自动存档阶段的对象重叠。
- 读入新存档后清理不属于当前存档的 Blob/Object URL 运行时缓存，并增加缓存字节统计；不删除 IndexedDB、桌面镜像、云备份或相册条目。
- 保留最近 20 回合正文渲染窗口，继续通过“继续渲染更早 20 回合”按需查看历史。

### 输入与正文显示

- 移动端 Enter 插入换行，点击发送按钮发送；桌面端继续使用 Enter 发送、Shift+Enter 换行。
- 增加中文输入法组合态保护，避免候选词确认时误触发发送。
- 用户气泡保留内部换行，并对长单词和长 URL 自动断行。

### 验证与兼容

- GitHub 云存档继续使用 v2 完整打包、分卷上传、原子发布和下载合并去重链路。
- 存档格式、存档树规则、云备份格式和相册持久化数据保持兼容。
- 本地定向回归、TypeScript 检查和生产构建通过；移动真机 Heap/Performance 复核仍需单独完成。

---

## v1.2 存档稳定与完整云备份修复版 - 2026-07-17

本次更新集中修复大型存档下索引恢复卡住、保存删除无响应、内存持续增长，以及 GitHub 云存档上传到一半停止的问题。存档模型重新统一为“一份存档对应一棵树”，本版本不包含地图分支内容。

### 存档目录与恢复

- 存档列表只读取轻量目录，不再为了确认索引完整而反序列化全部完整存档。
- 索引缺失时一次只读取一个节点；同一页面、应用窗口和多个同源标签页不会重复运行恢复任务。
- 保存和删除操作优先于后台目录恢复，避免只读扫描长期占用事务后出现“保存不了、删除没反应”。
- 读取存档不再隐式创建新的保护节点。已有旧 `backup` 节点不会在升级时自动删除，会作为历史恢复点继续展示并允许主动清理。
- “全部”页签会显示全部正常节点；导入节点和旧历史恢复点不再混用“保护存档”概念。

### 存档树节点轮转

- 存档树总数不设上限，系统不会因为玩家拥有多棵树而删除旧树。
- 每棵存档树分别保留最多 5 个手动节点和 6 个自动节点，两种额度独立计算。
- 新节点确认写入成功后，才删除当前树中最旧的同类节点；保存失败不会提前损失旧进度。
- 仍被差量节点引用的旧完整节点只会转为不可见基底，保证保留节点可以正常恢复。
- 升级不会立即批量删除已有超额节点；玩家后续正常保存时，当前树才会按新规则安全轮转。

### GitHub 完整云备份

- 云存档不再按回合节点逐个打包、逐文件提交，而是先在本机生成一份完整逻辑备份，再开始网络上传。
- 节点与资源一次只处理一个，图片按 SHA-256 去重，压缩与校验交给 Worker，并以约 8 MiB 分卷写入独立临时数据库，避免全部存档、Base64 和分卷同时占用内存。
- 所有分卷上传完成后，通过一次 Git Tree / Commit / Ref 更新原子发布；中途失败或取消时，旧云备份仍然有效。
- GitHub 请求增加超时、有限重试、指数退避、`Retry-After`、主限流等待和取消，避免单个请求永久悬挂。
- 下载会逐卷校验后与本地合并去重，不再清空本地存档；重复节点和资源跳过，节点 ID 内容冲突时保留本地，并把云端冲突树整体重映射为新树。
- 旧版逐节点云存档继续支持下载合并；下一次成功上传后升级为新版完整备份格式。

### 当前说明

- 本版本不包含地图分支内容。
- 云端恢复是“合并”，不是“覆盖”；重复下载同一份备份时，已存在的相同节点不会再次导入。
- 构建时仍可能出现既有大 chunk 警告，不影响本次存档修复和部署。

---

## v1.1 稳定性修复版 - 2026-07-14

本次更新集中处理玩家反馈的闪退、后台切换、接口兼容、重 roll、相册卡顿、手机知情边界和 NPC 关系错位等问题。地图功能不在这个分支中，本版优先把现有主流程修稳。

### 运行稳定性

- 页面切到后台或切换标签后，流式正文和后台任务会先缓冲状态，回到前台后再统一刷新，降低浏览器因高频渲染和内存峰值而白屏、刷新或闪退的概率。
- 增加工作流恢复与错误边界保护。主剧情、变量、新闻、记忆、自动生图等流程异常时会留下可恢复状态，页面模块加载失败也会提供重新加载入口。
- 首页与大型系统页面改为按需加载并增加加载失败重试，减少首次进入和长期游玩时的主线程压力。
- 存档运行时数据继续压缩，避免调试快照、重复上下文和图片内容不断堆积。

### API 与生成流程

- DeepSeek 兼容链路重新整理：保留 OpenAI 兼容协议，同时针对 reasoning、空 content、格式缺失和中转站差异增加恢复与重试，不再因一层兼容判断直接禁用 DeepSeek。
- Claude 兼容、Gemini 地址归一化、OpenAI 兼容模型列表和连接测试逻辑继续修复，减少“连接测试成功但正式请求失败”或地址重复拼接的问题。
- 重 roll 改为使用当前回复生成前的独立快照，避免最近上下文层数调整后重 roll 串入错误回合、重复提交变量或污染后续状态。
- 主剧情与后台任务增加中断、恢复和重复提交保护，降低切换页面或请求失败后流程卡死的概率。

### 相册、参考图与图片性能

- 合并角色图库与参考图工作区，角色头像、参考图和生图入口集中管理，左侧角色列表图标占位也已统一。
- 参考图注入增加全局开关，默认关闭；各生图界面会明确显示当前是否启用参考图。部分中转供应商不支持参考图，失败时可直接关闭。
- 参考图现在计入图片资源统计，导入、导出、删除和批量整理改为分批处理与 Worker 归档，降低大量图片操作时严重卡顿或白屏的风险。
- 相册内容按真实图片内容去重，同一张图片即使文件名或引用不同，也不会重复保留两份资源。
- 图片任务会显示来源与用途，角色视觉、正文插图、场景图、手机背景和参考图更容易区分。

### 手机、剧情与记忆

- 修复手机系统提示词上下文未注入玩家自定义提示词的问题，并把手机世界书、NPC 私有记忆和近期主剧情检索接回同一条上下文链路。
- 私聊回复调整为 4-8 条，群聊调整为 12-30 条；新增内置手机通讯文风，要求保留 NPC 角色底色、日常聊天感和不同场景下的语气变化。
- 手机 NPC 只知道自己经历、被告知或公开新闻中的事实；近期主剧情五回合内若命中当前聊天角色，会只补入该角色相关回合，避免不在场角色全知。
- 剧情编织的自定义提示词、持久化行为和记忆注入继续修复，减少重新载入后配置丢失或旧剧情内容回流。

### 好感度、关系与 NSFW 档案

- 好感度范围统一为 `-50～150`，关系阶段固定为：敌对、陌生、初见、熟识、知己、生死挚友；关系阶段由前端根据好感度自动派生，不再出现 `+75` 仍显示“点头之交”。
- 新增独立的“亲密关系”状态。它属于普通关系系统，不受 NSFW 开关隐藏，也不会仅凭高好感自动触发；只有正文明确建立或解除关系时才更新。
- 变量模型、事实解析、命令执行、旧存档迁移、主剧情、手机、关系规划和伙伴界面已经同步使用新关系规则。
- NSFW 总开关继续只负责提示词、档案、读取写入和生图联动隔离，不作为正文内容审核开关；关闭时已有档案保留。
- 重要 NPC 在 NSFW 开启时会建立可用的空档案基线，不再依赖模型是否刚好输出启用字段。
- 黑塔、大黑塔、Herta、The Herta 统一为同一身份并解除人偶关键词误拦截；身体档案指向大黑塔真实身体。普通人偶、机械造物、帕姆、佩佩、史瓦罗、怪物与裂界生物仍保持隔离。

### 设置、存档与界面

- 设置导航图标重新对齐，修复不同图标尺寸导致的列表跳动和视觉不整齐。
- 修复删除存档时静态断言失败的问题，并继续加固存档导入、导出和删除流程。
- 版本号更新为 `v1.1`，作者与贡献者信息更新为“作者：牢凌 · 贡献者：11MOMO，Penna Mch”。

### 当前说明

- 本版本不包含地图分支内容。
- 变量模型每回合可能遗漏部分语义字段的通用问题仍在继续评估；本版只把好感度、关系阶段、亲密关系和 NSFW 档案中可确定的部分改为前端规则，不增加第二套变量 API。
- 计划、记忆、Bug 工作清单和内部设计文档不随发布分支上传。

---

## v0.6 测试版 - 2026-06-05

### 重点更新

- 主剧情与 DeepSeek 适配：
  - 新增 DeepSeek 主剧情模式，默认关闭；可在游戏设置中切换标准模式或锁格式模式。
  - 标准模式会为 DeepSeek 增加主剧情格式保护，锁格式模式会进一步要求从思维链开始输出，降低只剩正文、丢失思维链和记忆段落的概率。
  - 主剧情请求增加 DeepSeek 协议校验与自动重试，若思维链、正文、记忆或变量草稿缺失，会在请求上下文中显示诊断。

- 修复了开启流式但是仍然是非流式的问题。

- API 接口设置保存优化，现在 API 设置从存档中分离为完全本地存储，不会再出现外面配置好后进入存档就消失的问题。

- OpenCode Zen 已适配，为单独的 API 选项：
  - OpenCode 模型列表和请求地址增加专门处理，兼容官方 Zen 路径与常见 Base URL 填写误差。

- 重点更新：NPC 记忆与伙伴档案重制优化：
  - NPC 档案升级为更明确的账本结构，支持总结、同行记忆、必须承接内容、禁止遗忘内容、未解决事项和最近互动。
  - 剧情会把相关 NPC 账本整理进主剧情上下文，让角色能承接更久远的承诺、关系、冲突和共同经历。
  - 变量模型增加 NPC 账本写入能力，用来把新发生的关系变化、约定和未结事项沉淀下来。
  - 伙伴界面改为玩家视角优先，默认只展示总结记忆和同行记忆；偏数据化的必须记得、禁止遗忘等内容仅在开发者模式下查看。

- 智库优化：
  - 智库模型原始返回会单独显示，方便判断本轮是否真的调用模型、是否返回有效编号、是否回退本地规则召回。
  - 剧情编织的内置主线入口已独立维护，清理智库主线页后不再误伤黑塔空间站、雅利洛、仙舟、匹诺康尼等内置剧情编织系列。

- 聊天正文与显示修复：
  - 修复历史元标签泄漏成角色气泡的问题，例如“历史时间 / 12”不应再出现在正文顶部。
  - 无前缀旁白不再被渲染成异常暗色，旁白显示会更一致。
  - 当模型把多个角色标签挤在同一行时，系统会自动拆成多行，减少一整段混在一起的阅读问题。
  - 修复行动选项异常消失相关问题，并对旧回合操作链路做了回归保护。

- 手机系统继续优化：
  - 私聊和群聊列表可分开查看，待处理来信可折叠。
  - 打开私聊或群聊时自动滚动到最新消息。
  - 群聊支持手动改名、查看成员、从头部快捷按钮拉人入群。

---

## v0.5.2 测试版 - 2026-06-04

这次更新主要处理近期积累的系统整理与体验优化：手机群聊更像真实群聊，旧装备系统开始退出主流程，战技系统改得更轻，智库与世界书也补上了更稳定的角色和力量标尺。

### 重点更新

- 手机群聊体验优化：
  - 打开私聊或群聊时会自动滚动到最新消息，不再每次从顶部手动下滑。
  - 群聊主动来信现在会生成 12-20 条消息，并要求至少 3 位成员参与发言，避免群聊变成单人连续刷屏。
  - 私聊和群聊列表分开显示，可在手机里切换“好友 / 群聊”。
  - 群聊支持手动改名、查看成员，并可从成员区拉人入群。
  - NPC 主动拉群时，群名会按成员和组织生成，不再直接显示“某某拉人入群”这类事件标题。

- 装备系统逐步移除：
  - 旧装备面板和装备变量链路已清理，避免主剧情继续围绕装备槽位运行。
  - 背包和随身物仍保留，主剧情可继续使用玩家携带物、环境、命途和行动来表现能力。

- 战技系统调整：
  - 普通战技槽位从 3 个精简为 1 个自制战技。
  - 命途战技保留。
  - 主剧情不再鼓励临时编造大量技能，更强调命途、随身物、环境和身体动作的结合。

- 智库内容整理：
  - 删除早期错误的智库 NPC 页、道具页、系统页预设，减少错误资料干扰。
  - 主线剧情已经交给剧情编织系统承接，智库不再承担主线剧情文本推进。
  - 智库召回思维链重写为分步分析：上下文、在场角色、预期登场、场景资料、门禁、相关性和自检，减少关键词相似导致的误召回。

- 新增常驻世界书“力量体系总览”：
  - 作为主剧情和命途觉醒的常驻战力标尺注入。
  - 明确“敌百 / 小型舰队 / 编队 / 城市军力”等参考对象是无命途普通士兵、普通军队和常规军力。
  - 不再允许把“一人敌百”误写成一个人能打一百个命途行者。
  - 保留浅涉、践行、深诣、伪令使四档阶段，用于约束战斗表现但不作为正文数值面板。

### 当前说明

- 本次更新偏系统整理和底层稳定性，短期内可能感觉“玩法按钮少了”，但主剧情会更少被旧装备、错误智库页和混乱战力标尺干扰。
- 如果遇到角色 OOC，仍建议优先查看左侧召回摘要和设置里的上下文诊断，确认是没召回、召回错，还是正文没有执行召回内容。
- 构建时仍可能出现既有大 chunk 警告，主要来自主包和内置剧情编织预设体积，不影响正常游玩。

---

## v0.5 测试版 - 2026-06-04

这次更新主要围绕“主剧情上下文更稳、角色不 OOC、召回更可见”展开。重点不是增加新玩法，而是把记忆、忆庭、智库和主剧情注入结构重新整理，让正文更容易承接前文和角色设定。

### 重点更新

- 记忆系统升级为即时 / 短期 / 中期 / 长期四层结构，旧存档会自动补齐中期记忆字段。
- 主剧情上下文注入开始去重：即时剧情回顾承担主要近期承接，短 / 中 / 长期记忆负责更长期的经历沉淀。
- 忆庭命中剧情回忆时，会暂停普通短 / 中 / 长期记忆注入，减少重复上下文挤占主剧情注意力。
- 主剧情请求上下文诊断增强，可以查看真实注入顺序、智库召回、忆庭召回和剧情编织注入情况。
- 智库角色召回重做：不再因为地点名误召角色，例如“黑塔空间站”不会自动召回黑塔本人。
- 新增“预期登场角色”召回：列车、车厢、广播、列车长等信号出现时，会提前召回帕姆人格，避免角色首次登场 OOC。
- 单主角模式增加星 / 穹门禁：选择星时不召回穹的人格资料，选择穹时不召回星的人格资料。
- 智库角色相关资料上限提高到 10 条，适配崩铁多人同场演出。
- 智库召回 CoT 强化为上下文故事分析：会先判断前文出现了谁、当前可能登场谁、哪些角色需要提前校准。
- 修正帕姆资料：补充第三人称自称“帕姆”、乘客称呼、列车长权威、嘴硬 / 装忙、车厢维护、调温、照顾植物等原著口吻与日常锚点。
- 左侧窗口改为“召回摘要”：默认显示智库召回和忆庭记忆召回的短条目，点击“完整”才展开完整内容。
- 左侧召回窗口现在会在忆庭 / 智库检索完成后立即刷新，不再等正文生成结束。

### 明日待办

- 新增“智库执行层”：把智库资料进一步整理成本回合必须落实的写作清单，让正文不只是看到资料，而是主动执行角色口吻、动作、禁写边界和地点机制事实。

### 当前说明

- 本版仍保留完整智库注入、智库诊断和忆庭上下文；左侧窗口只做快速摘要。
- 如果发现角色仍 OOC，建议优先查看左侧召回摘要和上下文查看中的完整智库召回，判断是没召回、召回错、还是正文没有执行。
- 构建时仍会出现既有大 chunk 警告，主要来自主包和内置剧情编织预设分包，不影响本次部署使用。

---

## v0.4.7 测试版 - 2026-06-03

这次更新集中处理最近玩家反馈的一批问题：接口保存、百度 API、在线人数、手机通讯录滑动、旧存档联系人为空、剧情编织锚点错位，以及“极其”污染词在上下文里反复繁殖的问题。

### 重点修复

- 修复各个接口设置页“保存”失效的问题：设置页现在会在修改后同步写入本地设置缓存，避免关闭窗口后丢失。
- 适配 Claude 兼容模式，补上独立开关与消息结构兼容，避免影响其他 OpenAI 兼容接口。
- 优化百度千帆 / Coding Plan 接口：兼容 `/v2`、`/v2/coding` 和完整 `chat/completions` 地址，模型列表获取会优先走官方模型接口并兼容当前 Base URL。
- 修复主界面“在线开拓者”统计偏本地的问题：接入 Cloudflare KV 后，进入游戏后的玩家也会继续发送在线心跳。
- 修复移动端手机功能里短信列表、通讯录好友列表无法滑动的问题。
- 新增“额外功能”设置页，加入污染词清理；默认清理“极其”，会在正文进入聊天历史和后台上下文前切断污染词继续繁殖。
- 修复剧情编织原著锚点错位：读档和回合结算时，会结合最近正文、玩家输入和当前地点判断是否需要跨原著系列纠偏。
- 修复旧存档手机联系人为空的问题：联系人为空时，会从近期正文中兜底识别已登场或常见同行角色，不覆盖玩家已有联系人。
- 修复存档体积异常膨胀：自动存档、手动存档、备份存档和导出存档包会清理运行期调试上下文，降低旧存档几十 MB、读档卡顿或分享包过大的风险。

### 当前说明

- 百度 Coding Plan 玩家建议把 Base URL 填为 `https://qianfan.baidubce.com/v2/coding`；普通千帆可填 `https://qianfan.baidubce.com/v2`。
- 如果旧相册或旧存档里已经有错误数据，系统会尽量兜底修复；无法修复的旧破图或异常条目仍建议重新生成或删除。
- 如果玩家关闭变量更新，NPC 档案、手机来信、变量事实仍会明显变弱；完整体验仍建议开启变量更新。
- 清理运行期快照后，读档后不能再重 roll 读档前的最后一条旧回复；当前回合内的重 roll 不受影响。

---

## v0.3 测试版 - 2026-05-29

这次更新合并了昨晚 `v0.2.1` 的收尾内容、今天下午到凌晨的剧情编织重做，以及一整轮玩家反馈的关键 bug 修复。  
简单说，这是一次偏“地基重修”的版本：主剧情不再被剧情编织强行拖着走，记忆、变量、手机、存档、reroll、NPC 档案这些会影响长期体验的系统都做了加固。

这批改动跨度很大，也确实花了很久：从昨天下午到今天凌晨，我们围绕“主剧情连续性”和“长期游玩稳定性”连续排查、重构、补测试，目标是让后续测试不再被同一类失忆、串档、重复剧情、变量污染问题反复打断。

### 一句话总结

- 剧情编织从“强制玩家走主线”改成“原著剧情参考与章节锚点系统”。
- 主剧情优先承接玩家输入、已发生事实、即时剧情回顾、剧情回忆和 NPC 记忆。
- 已归档剧情不会再轻易回流成当前事件，避免打过的 boss、完成的桥段被重新演一遍。
- reroll、存档、手机、记忆、变量等运行时副作用补上回滚和隔离。
- 变量系统进一步减少对玩家档案的干扰，并增强 NPC、手机、NSFW 档案等事实落库能力。
- 手机端 UI 继续补齐，记忆系统等面板在窄屏下不再挤成一团。

---

## 重点更新：剧情编织系统重做

### 原著剧情从“强推主线”改为“参考辅助”

旧版剧情编织最大的问题，是它容易把原著剧情当成当前回合必须发生的内容。玩家明明已经用自己的方式解决了某个桥段，后续主剧情到了原著对应位置时，仍可能再次把旧事件拉回来。

本版调整后：

- 原著剧情主要作为参考素材和章节方向，不再直接覆盖玩家当前行动。
- 玩家已经完成的事件会被视作历史事实，而不是等待复演的原著节点。
- 当前回合会优先承接玩家正在做的事，再参考剧情编织给出的章节目标、人物关系、未结问题。
- 剧情编织注入会区分“当前段”“后续段”“已归档段”，避免把未来或过去的段落混进当前正文。

### 引入已分解剧情预设

我们替换并接入了已分解的内置剧情编织预设。  
这次测试也确认了一个关键结论：之前剧情不推进、结束判定困难，很大程度上来自原始剧情素材没有按段拆好。

现在的内置预设具备：

- 系列、章节、分段结构。
- 当前段目标。
- 结束条件。
- 关键角色。
- 关键地点。
- 可承接的历史事实。
- 后续段落提示。

这让剧情编织不再只是一大段原著文本，而是更适合游戏运行时判断的章节材料。

### 自动推进逻辑重做

剧情编织推进不再只靠“当前回合写到了某个关键词”粗暴判断，而是加入了更清楚的阶段判断：

- 当前分段是否已经出现明确收束。
- 玩家行动是否真的推进了该段，而不是只是提到相关词。
- 后续分段是否有足够命中。
- 当前段是否已归档。
- 旧锚点是否需要自愈。
- 门禁结果是否允许写回锚点。

如果暂时不推进，面板会显示最近判定理由，方便继续调试。

### 已归档剧情防重演

这次重点修了“已经发生的原著段落被重新拿出来演”的问题。  
例如“某个 boss 已经被击退”这类内容，现在会作为历史事实承接，而不是再次生成一只同样的 boss 让玩家重复打。

机制上做了几层保护：

- 已归档段不会再作为当前注入段。
- 当前素材块会切到后续段。
- 旧段概括不会残留在当前块里。
- 可以保留“已经完成”的历史事实，用于 NPC、新闻、手机、主剧情承接。
- 状态自愈只修正锚点，不会伪造新的剧情进展记忆。

### 剧情编织与其他系统联动

这次没有只改剧情编织本体，而是把周边系统也接到同一套章节锚点上，避免各系统各讲各的。

- 主剧情：读取当前进度、历史归档、当前段软目标，但不得被原著强制绑架。
- 新闻系统：只承接已发生的公共事实和余波，不提前宣布未来剧情。
- 手机系统：可以根据已发生事件触发合理来信，但不能替玩家推进章节。
- NPC 记忆：剧情段落完成后，会给相关且有承接价值的 NPC 写入同行记忆。
- 即时记忆 / 忆庭：章节进度会沉淀为可召回事实，避免后续只靠聊天滑窗记住。
- 调试上下文：每回合请求上下文会记录剧情编织诊断，方便确认模型到底看到了什么。

### 剧情规划与关系规划

本版加入了更像“调度快照”的规划分析：

- 剧情规划关注当前章节、锚点、已完成段、待解问题和后续建议。
- NPC 关系规划不再绑定“女主路线”，而是面向所有重要 NPC。
- 关系规划依据共同经历、承诺、冲突、信任、称呼和同行记忆。
- 关系规划从常驻信息块移到独立 tab，避免把同行记忆全文塞进右侧栏。

这部分的目标不是替玩家决定路线，而是帮助系统理解“哪些关系值得继续承接”。

---

## 重点修复：reroll 与回合副作用

### 修复 reroll 后正文不变的问题

玩家反馈：重 roll 后，新正文和旧正文一模一样。  
进一步排查发现，不只是正文没有变化，变量、记忆等副作用也没有正确回退。

本版修复：

- reroll 前保存完整深拷贝快照。
- reroll 时恢复聊天、旅人、世界、记忆、忆庭、智库、手机、NPC、相册、新闻、剧情、剧情编织、变量批次、队列任务和回合数。
- reroll 请求增加随机扰动，避免直接复用旧回复。
- 旧后台流程如果在 reroll 后继续返回，会被工作流版本号挡掉，不能再把旧副作用写回状态。

### 修复 reroll 后忆庭编号跳号

玩家反馈：reroll 后忆庭回合编号跳号，被 reroll 掉的即时记忆仍被保存。  
本版把 reroll 和后台结算链路做了统一保护：

- 记忆压缩写入前检查当前工作流是否仍有效。
- 忆庭入库前检查当前工作流是否仍有效。
- 新闻、变量、剧情编织推进、NPC 记忆压缩、自动存档等后台结算全部加上过期保护。
- turnCount 持久化也必须确认工作流仍有效。

现在 reroll 不再只是“换正文”，而是会把该回合相关副作用一起回退。

---

## 重点修复：存档隔离与手机串档

### 修复多个存档内容互串

玩家反馈：A 存档的信息会出现在 B 存档里。  
排查发现，手机系统曾经存在全局备份，会在读档时和当前存档合并，导致通讯录、聊天记录、主动来信种子跨档污染。

本版修复：

- 手机通讯录、聊天、主动来信种子只属于当前存档。
- 读档时手机状态只来自 `save.手机`。
- 不再用全局 `phoneSystemState` 合并当前存档。
- reroll / abort 回滚时也不再写全局手机备份。
- 存档归一化只修当前存档本身，不拉入其他档案的运行时数据。

现在手机聊天记录和通讯录不会再跨存档互串。

### 明确运行时事实不能写入全局 setting

本次也统一了边界：

- 记忆。
- 忆庭。
- NPC。
- 新闻。
- 手机通讯录与聊天。
- 主动来信种子。
- 变量批次。
- 当前剧情进度。

这些都属于单个存档的运行时事实，不应进入全局设置。

---

## 重点修复：变量系统与玩家档案保护

### 玩家手写档案不再被变量系统覆盖

玩家反馈：自己认真写的外貌、性格、身份、背景会被变量系统改掉。  
本版明确保护玩家核心档案：

- `旅人.身份`
- `旅人.外貌`
- `旅人.性格`
- `旅人.背景`
- `旅人.能力`
- `旅人.专长知识`
- `旅人.穿着`

变量模型即使输出旧命令，也会在进入执行链路前被静默过滤。  
事实协议里的 `traveler_profile` 也不再作为红色警告刷屏，而是作为已忽略记录处理。

这能同时做到两点：

- 玩家手写内容不会被 AI 自动替换。
- 变量系统少处理一类高风险、低收益内容，减少噪音。

### 变量事实协议继续加固

变量系统仍保持“主剧情给事实草稿，变量模型提取事实，前端确定性转换命令”的方向。

本版继续加固：

- NPC 事实支持稳定 id 和中文姓名互查。
- NPC 记忆支持更稳的摘要写入。
- 手机来信种子能补全目标 NPC id。
- 时间推进继续遵守当前时间锚点，不把时间往回写。
- NSFW 档案新增独立事实类型。
- 旧 `<变量更新>` 仍保留兼容，但不再是主路径。

---

## 重点修复：NPC 记忆与角色连续性

### 强化 NPC 记忆写入法则

昨晚 `v0.2.1` 中补上了“开拓轶事版 NPC 记忆写入法则”，这次合并进 `v0.3`。

现在变量模型在每次有效互动后，需要审计：

- 是否要追加同行记忆。
- 是否要刷新关系。
- 是否要调整好感。
- 是否要记录称呼变化。
- 是否要记录同行状态。
- 是否要更新稳定档案字段。

这能减少“刚互动过几回合，NPC 就像没见过玩家”的情况。

### NPC 入档和查重更稳

- 未建档角色会先判断是否重要。
- 新入档时补写关键前情。
- 原著稳定 id 支持自动补档。
- 原创 NPC 不会因为匹配不到原著 canonical 而错误合并。
- 有记忆、好感变化、同行关系的重要 NPC 会倾向进入 companion 层，而不是很快掉成路人。

### NPC 记忆压缩提示词修复

修复并清洗了 NPC 同行记忆压缩提示词污染：

- 压缩结果必须是 NPC 自身可知范围内的自然中文。
- 保留稳定关系、称呼、承诺、亏欠、冲突原因。
- 不把压缩说明本身写进 NPC 记忆。
- 历史污染内容在注入主剧情时也会被清洗。

---

## 重点修复：手机系统

### 手机聊天会反哺主剧情

玩家反馈：第 20 回合和 NPC 在手机里聊天，第 21 回合主剧情里 NPC 不知道聊过什么。  
本版修复：

- 手机 AI 回复后，会立即写入一条即时记忆。
- 私聊对象绑定 NPC 时，会同步写入该 NPC 的同行记忆。
- 记忆来源标记为手机。
- 手机本地摘要仍保留，但不再是主剧情能知道手机内容的唯一入口。

现在手机聊天不是孤立小系统，而会成为后续主剧情可承接的事实。

### 主动来信不再长期沉默

玩家反馈：手机几乎不会触发 NPC 主动来信。  
本版修复：

- 变量模型会更积极审计低频 `phone_seed`。
- 主剧情回合结束后有低频兜底来信种子生成。
- 打开手机时会自动处理最高优先级 pending seed，生成真实短信。
- 来信种子尊重手机系统设置和每回合上限。
- 新增 `phone` 后台队列任务，能在变量面板看到手机来信任务。

主动来信不会变成每回合刷屏，但也不应该再长期完全不触发。

---

## 重点修复：正文归属、拟声词与“玩家夺舍 NPC”

这是一个反复修了多次的顽固问题：  
环境音、拟声词、生物吼叫、旁白短句，有时会被挂到玩家头像下面；NPC 的话偶尔也会被误判成玩家说话。

本版做了两层处理：

### 生成前约束

- 提示词继续要求旁白、环境音、动物叫声、机械声不能写成玩家发言。
- 玩家不能替 NPC 说话。
- NPC 台词必须归属到对应 NPC。

### 保存与渲染层清洗

仅靠提示词不够，所以本版也改了保存和渲染层：

- 增强 `playerSpeechGuard`。
- 增加“隆隆”“轰隆隆”等拟声识别。
- 旁白整句引号不再自动提升为玩家气泡。
- 只有玩家输入存在明确说话证据时，才允许把某些内容渲染成玩家发言。
- 新正文保存前会清洗错误归属。
- 清洗后的正文会进入后续记忆、变量、新闻、忆庭，避免错误归属继续污染系统。

这不是只改显示美化，而是尽量从正文保存链路上减少错误归属被长期记住。

---

## 重点修复：NSFW 档案

玩家反馈：开启 NSFW 档案后也不会更新。  
本版新增 `nsfw_archive` 变量事实协议，让 NSFW 档案不再只依赖旧变量命令猜路径。

新增能力：

- 支持 intimacyStage、boundaries、preferences、sensitivePoints、taboos、experiences、longTermFacts 等字段。
- 支持女性身体档案字段。
- 支持男性身体档案字段。
- 只更新已入档 NPC。
- 旧命令写 NSFW 档案时也会经过策略检查。

同时加入禁写边界：

- 帕姆。
- 佩佩。
- 怪物。
- 裂界生物。
- 反物质军团怪物。
- 机兵。
- 机器人。
- 人偶。
- 投影。
- 生物形态或非人对象。

这些对象暂时不进入 NSFW 档案系统。

---

## 新增：开局随机生成人设模板

玩家反馈：不会写外貌、性格、背景，希望有模板。  
本版在玩家信息页加入随机生成角色模板功能：

- 使用主 API 模型生成。
- 可生成外貌、性格、背景等开局档案。
- 玩家不满意可以再次生成。
- 旁边新增提示词输入框，玩家可以写偏好，例如“公司背景”“冷静理性”“战斗型”“黑塔空间站相关”等。
- 按钮旁增加小字提示：该功能走主 API 模型。
- 提示词要求生成的是玩家角色模板，不是弱路人 NPC 模板。
- 不默认让玩家取代星 / 穹，也不默认已经是列车组成员。

---

## 手机端 UI 适配补充

昨晚 `v0.2.1` 已经整理了一批手机端体验，本版继续补漏。

### 已包含的手机端适配

- 手机底部快捷菜单优化。
- 背包页手机布局调整。
- 剧情编织、智库、命途、装备、战技、存档、设置等面板窄屏优化。
- 开局档案、命途阶段、系统抽屉等多处窄屏细节优化。

### 本版新增补漏：记忆系统

玩家截图反馈：记忆系统在手机端仍沿用桌面左右双栏，右侧“记忆条目”被压成窄列。  
本版修复：

- 手机端改为纵向布局。
- 顺序为：记忆总览 -> 层级切换 -> 记忆条目 -> 阈值提示。
- 手机端禁止横向溢出。
- 桌面端仍保持原左右双栏。
- 新增 `test:memory-mobile` 回归检查，避免以后又退回桌面硬双栏。

---

## 调试与回归测试

这次不只是修功能，也补了不少回归检查，避免后面一改又坏。

新增或更新的检查包括：

- `test:story-weaving`
- `validate:story-weaving-canon`
- `test:reroll`
- `test:save-isolation`
- `test:prompt-context`
- `test:npc-memory`
- `test:phone-memory`
- `test:nsfw-archive`
- `test:player-speech`
- `test:traveler-profile`
- `test:traveler-template`
- `test:memory-mobile`

这些脚本覆盖了剧情编织、reroll、存档隔离、上下文注入、NPC 记忆、手机记忆、NSFW 档案、玩家发言归属、旅人档案保护、开局模板生成和手机端记忆 UI。

---

## 已验证

本轮关键改动已执行：

- `npm.cmd run test:memory-mobile` 通过。
- `npx.cmd tsc --noEmit` 通过。
- `npm.cmd run build` 通过。

构建仍有既有警告：

- Tailwind content 匹配范围偏宽。
- 部分 chunk 体积较大。
- `services/pathService.ts` 同时存在静态和动态导入。

以上为旧结构警告，不影响本次版本构建通过。

---

## v0.2.1 测试版内容已合并说明

昨晚 `v0.2.1` 主要完成：

- NPC 记忆写入法则。
- NPC 入档查重和字段边界收束。
- NPC 记忆压缩提示词修复。
- 手机主动来信机制增强。
- 手机来信种子尊重设置。
- 手机端适配收尾计划。

这些内容已全部合并进 `v0.3` 公告，不再单独作为正式测试公告分开发出。

## v0.2.0 - 2026-05-28

本次更新重点完成手机端体验整理，并修复若干会影响叙事连续性的关键问题。

### 新增

- 开局选择命途时，现在可以选择初始命途阶段。
- 可选阶段包括：浅涉、践行、深诣、伪令使、令使。
- 所选阶段会写入旅人命途档案，并进入开局全局事件、开局档案和总览确认页。

### 手机端适配

- 优化手机底部快捷菜单，减少系统面板和底部栏互相遮挡。
- 调整背包页手机布局，物品详情现在位于下方独立区域，不再与物品列表重叠。
- 优化剧情编织、智库、命途、装备、战技、存档、设置等面板在窄屏下的滚动、字号、按钮排列和内容密度。
- 保持桌面端布局习惯，手机端改动尽量通过响应式样式单独生效。

### 修复

- 暂停生成后，本轮已提交文本会回到输入框，方便玩家修改错字后重新发送。
- 终止生成时会回滚本轮临时消息，不再保存半截回复或错误推进回合。
- 纯拟声词、动物叫声和场景声音不再默认当作玩家说话。
- 手机回复现在会读取最近主剧情回顾和当前手机会话摘要，减少手机聊天与正文上下文脱节。
- 修复伙伴同行记忆压缩时误把压缩提示词写进 NPC 记忆的问题；历史污染内容在注入时也会被清洗。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- `npm.cmd run build` 通过。
